const initialState = [];
const productReducer = (state = initialState, action) => {
    switch (action.type) {
        case "FIND_ALL_PRODUCT":
            return state;
        default:
            return state;
            
    }
}
export default productReducer;